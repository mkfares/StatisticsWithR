#
# R Studio
#

# Projects
# Keep all related files together in one place
# Project icludes scripts, graphs, data, functions etc.
# File > New Project... > New Directory > New Project 
# - Specify the Directory Name
# - Specify where the directory will be located

# Save console code to a file
# Select History Tab from Upper right window 
# Click on the floppy disk icon to save the session code

# Souring a file - Execute the content of the file
# Open the file that contains the R code > Click on Source Button (or Command/Ctrl+Shift+Enter)

# Run code in the script pane
# Run a line of code: Click anywhere in the line > Click Run (or Command/Ctrl + Enter)
# Run multiple line of code: Select the lines > Click Run (or Command/ctrl + Enter)
# Run the entire script: Put the cursor anywhere in the script file/tab > Click Run (or Command/Ctrl+Shift+Enter)

# Run code in the console
# Type the code and press Enter
